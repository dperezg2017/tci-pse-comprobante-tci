spring:
  application:
    name: tci-pse-comprobante
  cloud:
    config:
      uri: "http://localhost:8888"
      fail-fast: true
      enabled: true

# DOCKER:
#spring:
#  application:
#    name: tci-pse-comprobante
#  cloud:
#    config:
#      uri: ${SERVER_CONFIG_URL:''}
#      fail-fast: ${SERVER_CONFIG_FAIL_FAST:true}
#      enabled: true
#
